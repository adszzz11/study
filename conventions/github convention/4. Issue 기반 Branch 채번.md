우리는 기능, 버그, 문서, 리팩토링 등의 작업 시 브랜치를 생성할 때, **GitHub Issue 티켓 번호를 기반으로 브랜치를 명명**하여 **추적성과 일관성**을 확보합니다.

### 🧩 GitHub Issues와 연동 시 자동 참조 가능

GitHub 이슈 번호 기반으로 커밋 메시지나 PR 제목에 `#이슈번호`를 넣으면 자동 링크됨:

예시:
```bash
git checkout -b feature/issue-42
git commit -m "✨ [#42] 로그인 API 구현"